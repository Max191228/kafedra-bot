meta:
  name: kafedra-vote-bot
  toolchain: python
  version: "3.11"

build:
  requirements: requirements.txt

run:
  command: python bot.py
  environment:
    - key: BOT_TOKEN
      value: "здесь_будет_твой_токен"
    - key: ADMIN_ID
      value: "здесь_будет_твой_айди"
